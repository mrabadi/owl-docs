# Compatibility matrix

This matrix describes the current executable foundation. “Preserved” means the original package data survives an unchanged Save As or a permitted local text patch; it does not imply that the editor can display or edit that feature. Exact unchanged Save As is additionally exercised against a reproducible 100-document public LibreOffice corpus. Editing claims remain limited to the explicit synthetic and targeted fixtures described below; this is not evidence of universal interoperability.

| Feature | Canvas/import behavior | Authoring behavior | DOCX save behavior | Classification |
|---|---|---|---|---|
| Editable Excalidraw figures | Figures reopen as ordinary semantic PNG pictures in every supported path. Owl Docs recognizes bounded, CRC-valid private iTXt scene metadata; a missing or stripped scene safely becomes an ordinary picture. | Insert launches the bundled offline editor; double-click, F2, context menu, and the Picture ribbon edit the selected figure. Save/Cancel, persistent Professional/Sketch modes, multiple independent image identities, ordinary sizing/wrapping, undo/redo, recovery, and clipboard use the existing picture lifecycle. | The authoritative fallback is a normal DrawingML PNG. The current 0.5.x slice keeps inert editable JSON in that PNG's private ancillary metadata, whose bytes survive tested native DOCX write/reopen. A future private OPC relationship/index remains planned for stronger external-edit detection and explicit flattening reports. | Editable self-contained vertical slice; broader M2 interop gates open |
| Basic body paragraph text | Direct body paragraphs retain their order around supported tables | Editable, including tabs and manual line breaks | New documents serialize text, `w:tab`, and `w:br`; some imported single-`w:t` paragraphs can be patched using their retained source mapping. Reopened paragraph-only files matching the current Owl Docs writer's complete canonical package grammar can be regenerated after supported edits. | Narrow editable subset |
| Font family/size, bold, italic, underline, strike, text color, highlight, superscript/subscript | Supported effective run values resolve through `docDefaults`, `basedOn` paragraph/character styles, theme fonts/colors, and direct formatting, including explicit false values, then transfer to the canvas | Rendered and editable as computed/direct formatting; sparse style-definition editing is not exposed | Serialized for new or explicitly simplified documents; preservation paths retain the original styles/theme parts | Supported computed-format subset |
| Named paragraph styles | Valid paragraph style IDs are retained. For safely resolved source styles, the semantic model separately tracks inherited text, paragraph-mark, and paragraph baselines plus exact direct-property masks; cyclic, missing, unsupported, and custom definitions do not receive an invented editable baseline. | Normal, No Spacing, Title, Subtitle, Quote, and Heading 1–9 are selectable outside tables. Normal follows editor font/size defaults. Enter after a heading starts its configured next style atomically; empty styled paragraphs retain their insertion appearance; direct run, paragraph-mark, and paragraph overrides survive repeated style changes and undo. Custom style definitions and named styles inside cells are not editable. | Supported built-ins emit `w:pStyle` and deterministic definitions. Effective values equal to the emitted Owl Docs definition are omitted unless their directness mask requires them; foreign source-inherited differences become direct values in a simplified copy so its visible formatting survives. Equal-by-value direct properties and explicit false/clear values in the current serializable OOXML vocabulary remain direct through supported save/reopen. Language metadata and a null No Highlight override retain provenance in the semantic model and recovery, but are not emitted as direct OOXML properties. Unsupported custom styles remain on preservation paths or are reported before a simplified copy. | Editable built-in catalog with provenance-aware round trip; custom styles preserved/view-only |
| Empty body-paragraph insertion format | Supported paragraph-mark character properties in `w:pPr/w:rPr` transfer to the semantic paragraph mark; absence means an empty format | Character formatting at a collapsed selection in an empty paragraph is durable after caret movement and becomes the format of later typed text. Deletion to empty and leaving an empty list item via Enter, Backspace, or list toggle preserve the active format, including explicit property clears. | Concrete supported properties and serializable clears are written to `w:pPr/w:rPr` and survive new-save/reopen. Language metadata and a null No Highlight override remain semantic/recovery-only; unchanged preservation paths retain the original XML. | Editable basic paragraph-mark subset |
| Paragraph alignment, indents, spacing, keep flags, page breaks | Basic direct paragraph values are transferred to the canvas; supported terminal hard `w:br` page breaks produce page boundaries rather than soft line breaks. A break followed by text in the same OOXML paragraph is outside this subset. | Rendered and editable; Ctrl+Enter and supported imported hard breaks paginate consistently on the canvas, in print, and in PDF; new documents default to single line spacing with no automatic paragraph gap | Serialized for new or explicitly simplified documents; safe-patch saves retain the original hard-break XML | Basic direct-format and hard-break subset |
| Page size, margins, orientation, sections | Ordered OOXML section ranges, break kinds, and per-section page settings are retained by the engine; the canvas currently uses its single page-geometry model | Normal, Narrow, Moderate, Wide, Office 2003, and four-sided custom margins are available for the canvas/print/PDF model. Multiple-section creation is engine/API-only. | New single-section documents serialize final body `sectPr`; the engine's targeted multiple-section API emits non-final paragraph `sectPr` plus the final body `sectPr` and reopens section order, break kinds, and geometry | Editable single-section UI; native multiple-section engine subset |
| Bullets and numbering | Supported `numbering.xml` abstract/instance definitions, paragraph `w:numPr`, counters, starts, level templates, suffixes, marker formatting, left marker alignment, indents, and tab stops resolve to semantic list data; adjacent paragraphs retain source list identity. Center/right marker alignment is conservatively reported as unsupported rather than rendered incorrectly. | Per-list metadata supports ten independently configurable bullet/text stops in space-equivalents (built-in bullet positions 0/4/.../36 and two-space gaps), a persistent “Use as Defaults” action, a context menu and contextual ribbon, aligned wrapped/soft-break lines, hierarchical counter restart/resequencing, continuation on Enter, and nesting on Tab/Shift+Tab. Numbered levels use decimal, uppercase alphabetic, uppercase Roman, lowercase alphabetic, and lowercase Roman styles, then repeat. | Authored levels 1–9 use standards-valid native `w:numPr` and generated `numbering.xml` definitions without literal marker text; supported formats, templates, starts, suffixes, and geometry reopen semantically. Editor level 10 uses literal marker text with equivalent tab/hanging geometry and reopens as level 10 in Owl Docs, but is not native continuation semantics in other editors. Broader numbering compatibility remains corpus-limited. | Native editable nine-level subset plus tenth-level visual fallback |
| Tables | Bounded simple rectangular, single-paragraph-per-cell text tables reopen as ordered semantic tables. Their authoritative grid widths and table alignment are used for layout; supported cells retain fills, per-side borders, padding, vertical and paragraph alignment, mixed direct run and paragraph-mark formatting, and Word-compatible automatic row metrics. Merged, nested, ragged, multi-paragraph, equation-bearing, image-bearing, and otherwise advanced tables use view-only paragraph fallbacks while original markup and media are retained. | Tables have bounded rows/columns, an optional header row, editable cells, body-order movement, and Tab/Shift+Tab traversal; Tab in the final cell appends one undoable row. An active cell or rectangular multi-cell selection accepts the normal character and paragraph-alignment controls, and empty-cell insertion formatting remains active after cell navigation or deletion to empty. A contextual Table ribbon supports row/column insertion and deletion plus 13 built-in plain, grid, light, medium, accent, banded, and dark-header styles. Merge/split, manual sizing, nested tables, and floating placement remain unsupported. | New and explicitly simplified documents serialize the supported structure, cell text, direct cell character/paragraph and paragraph-mark formatting, and selected built-in style as native `w:tbl`; the same semantic state is retained in recovery snapshots. Arbitrary imported widths, custom fills/borders/padding, and other presentation details outside this subset are not authored. Eligible paragraph-only edits to imports preserve their original table XML, relationships, and media exactly. | Editable rectangular text-table subset with native DOCX persistence; advanced tables preserved/view-only |
| Pictures and wrapping (Picture Layout v1) | Safe, bounded PNG/JPEG images reached through internal DrawingML relationships become semantic picture atoms when they use `wp:inline` or an exact supported canonical anchor. The two anchor forms use zero offsets for square and top-and-bottom wrapping, carrying top/right/bottom/left wrap distances, alt text, and either move-with-text (character/paragraph-relative origins) or fixed (page/page-relative origins). Inline distance values are preserved in the semantic record but are not visually applied. Oversized display sizes are proportionally reduced to available page width. External, path-traversing, missing, over-limit, or noncanonical anchors are not exposed as editable pictures; opaque source markup remains on eligible preservation paths. Raster signature/container checks and size/dimension/pixel/decode limits run before decode, but raster bytes are still decoded in the GUI process. | The Picture command inserts a native PNG/JPEG atom occupying one U+FFFC paragraph position. Picture Layout selects inline, square, or top-and-bottom placement, edits four wrap distances and the move/fixed relation for anchored layouts, disables those controls for inline pictures, and stores bounded alt text. Pictures support exact click/range selection, keyboard navigation, copy/paste of original bytes, deletion/replacement, a ratio-lockable size dialog, three persistable on-canvas resize handles (right, bottom, and bottom-right), revisions, previews, and undo/redo. Limits are 512 atoms, 16 MiB encoded per atom, and 32 MiB aggregate. Crop, rotation, z-order, arbitrary positioning, additional wrap modes, and pictures inside cells are unsupported. | New and eligible simplified-save documents write `wp:inline` or the canonical `wp:anchor` form with `wrapSquare`/`wrapTopAndBottom`, four distance attributes, the supported relative-position pair, and `wp:docPr@descr` alt text. Save/reopen and schema-v11 recovery retain bytes, type, dimensions, supported layout, alt text, and mixed text/equation/image order. Preservation paths retain unsupported original image parts and anchors without flattening them. | Editable bounded PNG/JPEG Picture Layout v1; other drawings preserved/view-only |
| LaTeX and Office Math | Supported OMML structures reopen as native equation atoms; unsupported Office Math remains opaque on eligible preservation paths | The non-executing LaTeX subset is canonicalized and rendered as vector math for rows, fractions, roots, scripts, sums, integrals, delimiters, and supported matrices | Supported equations serialize as native inline/display OMML and round-trip without literal LaTeX markers | Editable safe subset; unsupported OMML preserved/view-only |
| Headers and footers | Simple text and safe bounded inline PNG/JPEG drawings in the first default header/footer relationship are imported and rendered on every page; `{PAGE}` and `{PAGES}` represent supported page fields | The left, center, and right region of any page header/footer is directly clickable and editable; text and pasted pictures mirror across every page, while the editing controls and their fonts scale with zoom. Insert > Header & Footer enters the same on-page mode. | New and safely regeneratable Owl Docs documents use native header/footer parts, relationships, content types, center/right tab stops, PAGE/NUMPAGES fields, and DrawingML image relationships; pictures preserve source bytes and aspect ratio through save/reopen. Unsupported rich, first/even, linked, and section-specific variants remain on preservation paths | Editable aligned default text/picture subset; advanced variants preserved/view-only |
| Notes and comments | Not displayed | Not editable | Original parts remain opaque on preservation paths | Preserved/view-only |
| Charts, SmartArt, OLE, custom XML | Not displayed | Not editable or executed | Original parts remain opaque on preservation paths | Preserved/view-only |
| Tracked revisions, content controls, fields, hyperlinks | Visible text may be flattened; semantics are not rendered | Not editable as semantic objects | In-place edits in protected contexts are refused; original markup is preserved on preservation paths | Preserved/view-only or blocked |
| Macro-enabled DOCM/DOTM and DOTX templates | Rejected, even when renamed with a `.docx` extension | Not editable | Not saved | Explicitly unsupported |
| ActiveX, OLE, external relationships | Not executed or fetched | Not editable | Preserved as inert package data on eligible preservation paths | Inert opaque content |
| Strict and Transitional namespaces | Basic main-document parsing recognizes both namespace families | Same limited canvas authoring | New files are Transitional; broader Strict behavior is unverified | Parser foundation only |

## Save modes

- **Unchanged imported Save:** does nothing when saving back to the same path.
- **Unchanged imported Save As:** writes the retained original package bytes. A synthetic test verifies byte identity even after the source path is removed, and the external corpus gate opened and copied 100/100 pinned public LibreOffice fixtures byte-for-byte.
- **Safe imported text patch:** changes only the character data of a mapped, eligible `w:t` span. The mapper rejects structural edits, signed packages, unsupported text contexts, unsafe whitespace changes, and invalid text. Tests verify unrelated XML and an opaque package member remain unchanged; the corpus runner can apply the same locality check to explicitly configured external fixtures.
- **Reopened canonical simple document:** a paragraph-only DOCX matching the current writer's exact package, relationships, supported built-in style definitions, settings, body, and final-section grammar can be formatted or structurally edited and saved normally. The eligibility check rejects any extra part, external or altered relationship, unsupported/custom style definition, unsupported section setting, list/table/drawing/equation, or mismatch between serialized and current document defaults. Paired UI tests prove accepted save/reopen/style-transition behavior and byte-unchanged rejection when opaque content is added.
- **Simplified Save As:** after formatting or structural changes, the UI requires an explicit confirmation before writing the supported body subset—including paragraph-mark insertion formatting, native numbering, authored table structure, cell formatting, built-in styles, supported equations, and Picture Layout v1—and lists known categories that may be lost. It refuses the imported original path, aliases, and existing hard links as the destination.
- **New document save:** writes a macro-free Transitional DOCX containing body paragraphs with supported built-in `w:pStyle` identities, sparse direct `w:pPr/w:rPr` values and paragraph marks, native numbering definitions, authored native tables with the supported cell formatting and built-in styles, supported OMML equations, desktop-authored PNG/JPEG pictures in the Picture Layout v1 subset, deterministic used-style definitions and configured document defaults, plus a standard `settings.xml` default tab stop derived from the configured space-equivalent width. Separate engine/API tests also author and reopen ordered multiple sections; section authoring is not exposed by the desktop UI.

## Recovery compatibility

Recovery schema v13 persists the bounded semantic projection: text, direct formatting, paragraph-style identity, distinct inherited text/paragraph-mark baselines, exact direct-property provenance, paragraph-mark insertion formats, equations, supported body and header/footer picture bytes/dimensions/layout/alt text, tables, body-block order, default header/footer stories, stable identities, and page geometry. The decoder retains version 1–12 paths; repository migration fixtures cover versions 3–11. Missing paragraph-mark or header/footer data defaults to an empty format/story, missing Picture Layout v1 data defaults to safe inline placement, older style records default to provenance-free identity rather than an invented baseline, and v10 provenance derives the newly separated paragraph-mark baseline from its prior text baseline. Recovery does not contain an imported package's unrelated opaque parts, so a recovered import still requires a warned simplified Save As to a different destination.

## Optional Codex protocol compatibility

The `editor.v1` dynamic tools use an experimental `codex app-server` surface. Owl Docs sends `capabilities.experimentalApi=true` in `initialize`, reads the effective configuration, and disables every inherited MCP server by name in the per-thread config applied to both `thread/start` and `thread/resume`. Only then does it register the document tools. Invalid or incompatible configuration fails closed before authentication/model discovery. A fake-process controller test fixes this ordering and verifies that URLs, commands, and other server details are never copied into the override. When both Codex and Python are installed at configure time, an additional CTest check generates the installed runtime schema and requires the capability, configuration, dynamic-tool, and tool-call fields used by Owl Docs. That optional check is absent on offline hosts without those executables and is not a general compatibility guarantee. An absent or incompatible Codex runtime affects only optional chat; the editor remains usable offline.

Every release should expand this table only from corpus and round-trip evidence. A green unit-test suite is not evidence of universal Word parity.

Supported paragraph-mark `w:pPr/w:rPr` properties are part of the editable basic body subset. Unknown or unsupported direct `w:pPr`/`w:rPr` properties are reported as compatibility warnings and prevent a document from being classified as a fully basic body. Their original XML remains on preservation paths.
